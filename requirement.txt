面试宝阶段 1 运行与开发要求

[系统要求]
1. Windows/macOS/Linux 任一
2. Docker Desktop 4.0+ (含 Docker Compose v2)
3. 可选本地开发工具:
	- Python 3.11+
	- Node.js 20+
	- npm 10+

[后端依赖]
1. fastapi>=0.115.0
2. uvicorn[standard]>=0.32.0
3. pydantic-settings>=2.6.0
4. sqlalchemy>=2.0.36
5. psycopg[binary]>=3.2.3
6. asyncpg>=0.29.0
7. alembic>=1.13.3
8. pgvector>=0.3.6
9. passlib[bcrypt]>=1.7.4
10. dashscope>=1.20.14
11. tenacity>=9.0.0
12. circuitbreaker>=2.0.0
13. langchain-core>=0.3.17
14. pdfplumber>=0.11.4
15. python-docx>=1.1.2
16. paddlepaddle>=3.0.0
17. paddleocr>=2.8.1
18. python-multipart>=0.0.20
19. redis>=5.2.0
20. minio>=7.2.10
21. PyJWT>=2.10.0

[后端开发规范工具]
1. ruff>=0.8.0
2. pre-commit>=4.0.1

[前端依赖]
1. next>=14.2.0
2. react>=18.0.0
3. react-dom>=18.0.0
4. axios>=1.8.0
5. tailwindcss>=3.4.0
6. class-variance-authority>=0.7.0
7. clsx>=2.1.0
8. tailwind-merge>=2.6.0

[前端开发规范工具]
1. eslint>=8.0.0
2. prettier>=3.0.0
3. husky>=9.0.0
4. lint-staged>=15.0.0

[容器服务要求]
1. PostgreSQL + pgvector (镜像: pgvector/pgvector:pg16)
2. Redis 7 (镜像: redis:7-alpine)
3. MinIO latest (镜像: minio/minio:latest)

[启动命令]
1. 根目录执行: docker compose up --build

[验收标准]
1. 访问 http://localhost:3000 返回 Next.js 页面
2. 访问 http://localhost:8000/docs 返回 FastAPI 文档
3. 访问 http://localhost:8000/api/v1/health 返回统一响应格式
4. 后端存在统一异常处理器与 ApiResponse(code, message, data)
5. 前端存在 Axios 拦截器并支持 JWT 刷新
